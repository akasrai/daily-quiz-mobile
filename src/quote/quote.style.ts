import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  quoteWrapper: {
    flex: 0,
    padding: 20,
    marginTop: 30,
    alignItems: 'center',
  },
  quote: {
    padding: 5,
    color: '#fff',
    textAlign: 'center',
    fontFamily: 'Ubuntu-R',
  },
});
