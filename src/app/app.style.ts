import {StyleSheet} from 'react-native';

export const appGradientBG = ['#02183b', '#032862', '#fff', '#fff', '#fff'];

export const appStyles = StyleSheet.create({
  container: {flex: 1},
  flex: {
    flex: 1,
    alignItems: 'center',
    alignContent: 'center',
  },
  row: {
    flex: 0,
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
});
